# Set account-wide variables. These are automatically pulled in to configure in the root erragrunt.hcl.
locals {
  account_name   = "User"
  aws_account_id = "8166********"
  aws_profile    = "Tokyo"
  name_prefix    = "jessie-Tokyo"
}
